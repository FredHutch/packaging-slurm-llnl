packaging
=========

assorted files used for packaging pending transfer to
a git-buildpackage framework.

use
---

tag with version "slurm-llnl\_2.3.5p1"
when tagging push source and tags

    git push
    git push --tags
